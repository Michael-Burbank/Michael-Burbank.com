let oldArray = [1, 4, 2, 1, 6];

// GOAL: Return sorted array in ascending order. 

let newArray = [];
let position = 0;

function sort(oldArray){
    for(let i = 1; i <= oldArray.length; i++){
        for(let j = 0; j <= i - 1; j++){
            if(oldArray[i] < oldArray[j]){
                
            }
        }
    }
};